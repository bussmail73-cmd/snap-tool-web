{
  "name": "snapchat-profile-viewer",
  "version": "1.0.0",
  "description": "Snapchat Public Profile Viewer - Legacy Server (Use main server instead)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "express-rate-limit": "^7.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["snapchat", "profile", "viewer", "tool"],
  "author": "",
  "license": "MIT",
  "note": "This server is no longer needed. The main server (../server.ts) now handles all profile requests using RapidAPI."
}
